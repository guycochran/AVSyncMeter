import SwiftUI

enum VenueTheme {
    static let bg = Color(red: 0.05, green: 0.055, blue: 0.06)
    static let panel = Color(red: 0.10, green: 0.11, blue: 0.12)
    static let line = Color.white.opacity(0.12)
    static let dim = Color.white.opacity(0.55)
    static let early = Color(red: 1.0, green: 0.78, blue: 0.20)
    static let late = Color(red: 0.35, green: 0.78, blue: 1.0)
    static let stable = Color(red: 0.35, green: 0.85, blue: 0.45)
    static let unstable = Color(red: 0.95, green: 0.45, blue: 0.30)
    static let meter = Color(red: 0.25, green: 0.85, blue: 0.40)
}
